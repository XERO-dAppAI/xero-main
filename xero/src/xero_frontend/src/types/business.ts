export interface BusinessProfile {
  businessName?: string;
  business?: {
    name?: string;
    type?: string;
  };
  businessType?: string;
  businessCategory?: string;
  logo?: string;
  // Add other fields as needed
} 